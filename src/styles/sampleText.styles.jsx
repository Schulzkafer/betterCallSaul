import { StyleSheet } from 'react-native';
import colors from './colors';

const sampleTextStyles = StyleSheet.create({
   text: {
      margin: 10,
      fontSize: 20,
      textAlign: "center",
      color: colors.choco
   }
});

export default sampleTextStyles;